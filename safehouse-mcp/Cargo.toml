[package]
name = "safehouse-mcp"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Keyless stdio MCP shim: exposes safehoused's unix-socket RPC as MCP tools"

[dependencies]
anyhow = { workspace = true }
serde_json = { workspace = true }
