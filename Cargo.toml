[workspace]
resolver = "2"
members = ["safehoused", "safehouse-mcp", "spikes/qj-coldstart"]

[workspace.dependencies]
matrix-sdk = { version = "0.18", features = ["e2e-encryption", "sqlite"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Already vendored transitively via matrix-sdk-sqlite; used directly here for
# the mailbox read-cursor store (D17), not for any Matrix state.
rusqlite = "0.37"
